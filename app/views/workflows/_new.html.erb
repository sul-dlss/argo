<%= form_tag item_workflows_path do %>
  <div class='form-group'>
    <%= select_tag :wf, options_for_select(workflow_options, Settings.apo.default_workflow_option), class: 'form-control' %>
  </div>
  <button class='btn btn-primary'>
    Add
  </button>
<% end %>
