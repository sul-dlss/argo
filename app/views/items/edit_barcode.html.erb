<%= turbo_frame_tag 'edit_barcode' do %>
  <%= form_with model: @change_set do |f| %>
    <div class="mb-3">
      <%= f.label :barcode, class: 'visually-hidden' %>
      <%= f.text_field :barcode, class: 'form-control' %>
    </div>
    <div class="float-end">
      <%= link_to 'Cancel', solr_document_path(params[:id]), target: '_top' %>
      <%= f.submit 'Save', class: 'btn btn-primary' %>
    </div>
  <% end %>
<% end %>
