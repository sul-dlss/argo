<%= form_with model: @form, url: set_rights_item_path(@cocina.externalIdentifier) do |f| %>
  <div class='form-group'>
    <%= f.select :rights, f.object.rights_list, {}, class: 'form-control' %>
  </div>
  <button class='btn btn-primary'>Update</button>
<% end %>
