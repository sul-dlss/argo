<p><%= I18n.t('argo.content_type.update_explanation_single') %></p>
<p><%= I18n.t('argo.content_type.suggeted_mappings').html_safe %></p>
<p>More complex updates should be executed by hand on the datastream XML.</p>

<%= form_tag item_content_type_path(item_id: @cocina.externalIdentifier), id: 'content_type_form', method: :patch do %>
  <% display_content_types = [['none', '']] + Constants::CONTENT_TYPES.keys %>
  <% display_resource_types = [['none', '']] + Constants::RESOURCE_TYPES.keys %>
  <div class='mb-3'>
    <label>Old resource type</label>
    <%= select_tag :old_resource_type, options_for_select(display_resource_types, @old_resource_type), class: 'form-select' %>
  </div>
  <div class='mb-3'>
    <label>New content type</label>
    <%= select_tag :new_content_type, options_for_select(display_content_types), class: 'form-select' %>
  </div>
  <div class='mb-3'>
    <label>New resource type</label>
    <%= select_tag :new_resource_type, options_for_select(display_resource_types, @old_resource_type), class: 'form-select' %>
  </div>
  <button class='btn btn-primary'>Update</button>
<% end %>
