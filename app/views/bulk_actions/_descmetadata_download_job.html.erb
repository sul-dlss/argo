<% if bulk_action.present? && bulk_action.status == 'Completed' %>
  <%= link_to('Download Metadata', file_bulk_action_path(bulk_action.id, filename: Settings.bulk_metadata.zip, mime_type: 'application/zip')) %>
<% end %>
