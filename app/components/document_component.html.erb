<%= content_tag :div,
  id: @id,
  data: {
    'document-id': @document.id.to_s.parameterize,
    'document-counter': @counter,
  },
  itemscope: true,
  itemtype: @document.itemtype,
  class: 'document document-component' do %>
  <%= title %>
  <%= render 'catalog/show_embargo_sidebar', document: @document %>

  <div class="row">
    <div class="col-md-3">
      <%= render 'catalog/show_thumbnail', document: @document %>
    </div>
    <div class="col-md-9">
      <%= render ExternalLinksComponent.new(document: @document) %>
      <hr>
      <%= render SidebarControlsComponent.new(solr_document: @document, manager: helpers.can?(:manage_item, @cocina)) %>
    </div>
  </div>

  <%= render Blacklight::DocumentMetadataComponent.new(
      fields: @presenter.field_presenters,
      show: true
    ) %>
  <div class="accordion">
    <%= render 'datastreams_default', document: @document %>
    <%= render 'events_default', document: @document %>
    <%= render 'cocina_default', document: @document %>
    <%= render 'history_default', document: @document %>
    <%= render 'contents_default', document: @document %>
    <%= render TechmdComponent.new(result: @presenter.techmd) %>
  </div>
<% end %>
